{
    "id": "teambot",
    "version": "1.0.0",
    "name": "TeamBot - AI Agent Teams",
    "description": "Installs TeamBot CLI for autonomous AI agent workflows with GitHub Copilot",
    "documentationURL": "https://github.com/teambot-ai/teambot",
    "options": {
        "version": {
            "type": "string",
            "default": "latest",
            "description": "TeamBot version to install. Use 'latest' for main branch, or provide a version number (e.g., '0.2.0') which will be automatically prefixed with 'v' to match git tags (e.g., 'v0.2.0'). You can also provide a full git ref like 'v0.2.0' directly."
        }
    },
    "installsAfter": [
        "ghcr.io/devcontainers/features/python"
    ],
    "containerEnv": {
        "TEAMBOT_INSTALLED": "true"
    }
}
